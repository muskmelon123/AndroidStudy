<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lvhang.androidstudyproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".DatabaseStudy.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".StartActivityForResult.OtherActivity">
        </activity>

        <activity
            android:name=".StartActivityForResult.MainActivity">
        </activity>

        <activity
            android:name=".SearchView.MainActivity">
        </activity>

        <activity
            android:name=".GridView.MainActivity">
        </activity>

        <activity
            android:name=".DatabaseStudy.DataLockMainActivity">
        </activity>
    </application>

</manifest>
